name: windows-powershell

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

defaults:
  run:
    shell: powershell

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2

      - name: test3
        run: |
          ./1.build.ps1 test3

      - name: test2
        run: |
          ./1.build.ps1 test2
